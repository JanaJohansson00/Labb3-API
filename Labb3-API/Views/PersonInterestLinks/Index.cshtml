@model IEnumerable<Labb3_API.Models.PersonInterest>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        @* <tr>
            <th>
                @Html.DisplayNameFor(model => model.person)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.interest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.link)
            </th>
            <th></th>
        </tr> *@
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           @*  <td>
                @Html.DisplayFor(modelItem => item.person.PersonId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.interest.InterestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.link.LinkId)
            </td> *@
            <td>
                <a asp-action="Edit" asp-route-id="@item.PersonInterestId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PersonInterestId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PersonInterestId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
